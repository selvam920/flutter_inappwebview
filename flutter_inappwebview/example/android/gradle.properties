org.gradle.jvmargs=-Xmx1536M
android.useAndroidX=true
# Disabled: Jetifier was OOM'ing on this machine trying to rewrite the
# (large, native-lib-embedded) Flutter engine jars. Everything in this
# project's dependency tree is already AndroidX-native, so it isn't needed.
android.enableJetifier=false
android.nonTransitiveRClass=false
android.nonFinalResIds=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
